{

    // Lifecycle hooks
    "hooks": {
        // One local patch-version commit per merged Change, then rebuild indexes.
        // The bump target creates no tag, performs no push, and publishes nothing.
        "on_merged": "make version-bump-patch && make index",
    },
}
