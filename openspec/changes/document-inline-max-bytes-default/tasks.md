## Specification Tasks

- [x] Promote `specs/agent-exec-run/spec.md` delta — inline output 既定 max-bytes = 65536 を MUST 化
  Expected canonical result: canonical spec が「`run`/`start` の `--max-bytes` 既定は 65536 (64 KiB)」を明記、値変更は schema_version bump を伴う
- [x] Review scenario for default value observation

## Future Work

- なし
