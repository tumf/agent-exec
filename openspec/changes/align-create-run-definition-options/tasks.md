## Implementation Tasks

- [ ] Classify the current CLI flags into definition-time vs. launch/observation-time responsibilities in the lifecycle design and shared specs (verification: `openspec/changes/align-create-run-definition-options/design.md` and spec deltas explicitly separate persisted job-definition options from run/start observation options).
- [ ] Extend the `create` CLI surface in `src/main.rs` so it accepts the same definition-time options as `run` for shared metadata families, including repeatable `--tag` plus completion and output-match notification flags (verification: `src/main.rs` defines matching `create` arguments and clap validations parallel the `run` / `notify set` behavior for these options).
- [ ] Update the lifecycle and jobstore specs so `create` and `run` are required to stay aligned for persisted job-definition options, using tags and notification settings as concrete required examples (verification: `specs/agent-exec/spec.md`, `specs/agent-exec-run/spec.md`, and `specs/agent-exec-jobstore/spec.md` contain matching requirement language and scenarios).
- [ ] Persist create-time tags and notification configuration into `meta.json` without runtime side effects, and make `start` consume the saved values instead of requiring re-specification (verification: lifecycle/jobstore code writes the saved metadata before launch, `src/run.rs` or lifecycle start code reads it, and `tests/integration.rs` covers create-time persistence plus start-time reuse).
- [ ] Add implementation-facing guidance that future persisted metadata options must be wired through both `create` and `run` unless documented otherwise (verification: proposal/design text plus task evidence point to `src/main.rs`, lifecycle code, and `tests/integration.rs` as the places that must stay aligned).
- [ ] Add integration coverage when implemented so a job created via `create` and a job created via `run` persist equivalent metadata for shared definition-time options, including tags and notification settings, and so `create` itself never triggers delivery (verification: `tests/integration.rs` compares `meta.json` fields for both paths and includes no-side-effect notification cases).
