## 1. 統合テスト

- [x] 1.1 run/status/tail/wait/kill の統合テストを追加する（検証: `cargo test` で JSON-only stdout を確認）

## 2. CI マトリクス

- [x] 2.1 CI に `windows-latest` を追加する（検証: `.github/workflows/*` に Windows が含まれる）
