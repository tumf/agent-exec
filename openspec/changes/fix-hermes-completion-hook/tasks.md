## Implementation Tasks

- [x] Rewrite `skills/agent-exec/scripts/hermes-notify-hook` to invoke fail-closed `hermes send --quiet --to "$HERMES_NOTIFY_TARGET" "job_id=$AGENT_EXEC_JOB_ID event_path=$AGENT_EXEC_EVENT_PATH"`: require non-empty `HERMES_NOTIFY_TARGET`, remove `hermes notify` and all LLM provider/model handling (`HERMES_NOTIFY_PROVIDER`, `HERMES_NOTIFY_MODEL`), keep the `HERMES_BIN` override, and update the usage header to show the target assignment embedded in `notify_command` rather than managed-child `--env` routing (verification: integration - `bash -n skills/agent-exec/scripts/hermes-notify-hook` plus the `hermes_notify_hook`-prefixed tests in `tests/integration.rs`; verification-id: hermes-hook-tests)
- [x] Add deterministic tests in `tests/integration.rs`, all named with the `hermes_notify_hook` prefix, that execute the tracked helper script directly: (a) success path with a fake `hermes` executable (via `HERMES_BIN` or an isolated `PATH`) that records argv to a file, asserting the exact argument vector `send --quiet --to <target> <message>` where `<message>` is `job_id=<id> event_path=<path>` with no provider/model/LLM arguments; (b) missing `HERMES_NOTIFY_TARGET` exits non-zero without invoking the fake binary; (c) missing binary exits non-zero with `HERMES_BIN` unset, `PATH` set to a directory containing no `hermes`, and `HOME` set to an empty temporary directory so the helper's `$HOME/.hermes/...` fallback cannot resolve a real installation; no network, credentials, or LLM may be used (verification: integration - `cargo test --test integration hermes_notify_hook`; verification-id: hermes-hook-tests)
- [x] Update `skills/agent-exec/references/hermes.md` with an MCP `run` example whose `notify_command` embeds the request-scoped target assignment (e.g. `HERMES_NOTIFY_TARGET='slack:C0123:171234.0001' /absolute/path/hermes-notify-hook`), states that `notification.state="armed"` is persisted-sink evidence only, and keeps the single `wait --forever` watcher solely as the fallback when no sink is persisted; add a `hermes_notify_hook`-prefixed doc test in `tests/integration.rs` asserting no fenced code block in that file contains `hermes notify` (prose prohibiting the obsolete command is allowed) (verification: integration - `cargo test --test integration hermes_notify_hook`; verification-id: hermes-hook-tests)

## Future Work

- Durable retries and downstream delivery acknowledgements require a separate proposal.

## Final Validation

Archive validation is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-hermes-completion-hook --archive-gate`

## Current Acceptance Follow-up
- attempt: 1
- [x] [acceptance-rerun-cargo-filter-syntax] (minor) The declared change-blocking verification rerun command is not executable as written because cargo rejects its second positional test filter | evidence: openspec/changes/fix-hermes-completion-hook/proposal.md frontmatter verifications[0].rerun begins with `cargo test --test integration hermes_notify_hook notify_failure_does_not_change_job_state`; cargo 1.93.1 in this workspace exits with `error: unexpected argument 'notify_failure_does_not_change_job_state' found` before running any test, so the declared rerun chain returns non-zero without producing verification evidence; the intended tests all pass when filtered correctly: `cargo test --test integration -- hermes_notify_hook notify_failure_does_not_change_job_state` reports 5 passed and `cargo test --test mcp_integration mcp_run_reports_only_persisted_notification_as_armed` reports 1 passed | required_changes: openspec/changes/fix-hermes-completion-hook/proposal.md — Rewrite frontmatter verifications[0].rerun to valid cargo filter syntax, e.g. `cargo test --test integration -- hermes_notify_hook notify_failure_does_not_change_job_state && cargo test --test mcp_integration mcp_run_reports_only_persisted_notification_as_armed && bash -n skills/agent-exec/scripts/hermes-notify-hook`, or split the first segment into one cargo invocation per filter | verification: openspec/changes/fix-hermes-completion-hook/proposal.md — Execute the exact updated frontmatter rerun string verbatim from the repository root; it must run the hermes_notify_hook tests, notify_failure_does_not_change_job_state, and mcp_run_reports_only_persisted_notification_as_armed and exit 0, and `cflx openspec validate fix-hermes-completion-hook --strict` must still pass
  finding: {"evidence":["openspec/changes/fix-hermes-completion-hook/proposal.md frontmatter verifications[0].rerun begins with `cargo test --test integration hermes_notify_hook notify_failure_does_not_change_job_state`","cargo 1.93.1 in this workspace exits with `error: unexpected argument 'notify_failure_does_not_change_job_state' found` before running any test, so the declared rerun chain returns non-zero without producing verification evidence","the intended tests all pass when filtered correctly: `cargo test --test integration -- hermes_notify_hook notify_failure_does_not_change_job_state` reports 5 passed and `cargo test --test mcp_integration mcp_run_reports_only_persisted_notification_as_armed` reports 1 passed"],"id":"acceptance-rerun-cargo-filter-syntax","required_changes":[{"description":"Rewrite frontmatter verifications[0].rerun to valid cargo filter syntax, e.g. `cargo test --test integration -- hermes_notify_hook notify_failure_does_not_change_job_state && cargo test --test mcp_integration mcp_run_reports_only_persisted_notification_as_armed && bash -n skills/agent-exec/scripts/hermes-notify-hook`, or split the first segment into one cargo invocation per filter","file":"openspec/changes/fix-hermes-completion-hook/proposal.md"}],"severity":"minor","summary":"The declared change-blocking verification rerun command is not executable as written because cargo rejects its second positional test filter","verification":[{"description":"Execute the exact updated frontmatter rerun string verbatim from the repository root; it must run the hermes_notify_hook tests, notify_failure_does_not_change_job_state, and mcp_run_reports_only_persisted_notification_as_armed and exit 0, and `cflx openspec validate fix-hermes-completion-hook --strict` must still pass","file":"openspec/changes/fix-hermes-completion-hook/proposal.md"}]}
  evidence: proposal.md frontmatter verifications[0].rerun now reads `cargo test --test integration -- hermes_notify_hook notify_failure_does_not_change_job_state && cargo test --test mcp_integration mcp_run_reports_only_persisted_notification_as_armed && bash -n skills/agent-exec/scripts/hermes-notify-hook` (added `--` before the integration filters)
  evidence: ran that exact rerun string verbatim from the repo root: integration 5 passed (4 hermes_notify_hook + notify_failure_does_not_change_job_state), mcp_integration 1 passed, bash -n clean, EXIT=0; `cflx openspec validate fix-hermes-completion-hook --strict` printed "Validation passed" (exit 0)
