## Implementation Tasks

- [x] Add a shared expansion guard to compression result generation. Completion condition: `src/compress.rs` or equivalent code compares generated compressed stream text against the raw observed stream text and suppresses compressed text when it would be larger than or equal to raw. (verification: unit - add `src/compress.rs` tests that cover smaller, equal-size, and larger compressed candidate cases and run `cargo test compress`)

- [x] Return bounded fallback compression metadata when the guard triggers. Completion condition: guarded responses keep `compression` present for non-`off` modes, set `applied=false`, avoid large compressed `stdout`/`stderr`, and include an explicit strategy/reason such as `expansion-guard`. (verification: integration - `tests/integration.rs` asserts the fallback payload is shorter than the raw observed payload, includes the guard reason, and passes with `cargo test --test integration compression_expansion_guard`)

- [x] Preserve existing raw observation and `off` mode contracts. Completion condition: canonical `stdout`/`stderr`, range fields, total byte fields, and `encoding` remain raw and unchanged by the guard; `--compress off` still omits `compression`. (verification: integration - `tests/integration.rs` run/tail compression tests assert raw fields and `off` omission after guard implementation)

- [x] Cover the observed tail expansion risk with an integration regression test. Completion condition: an integration test tails a job whose command output is JSON/NDJSON-like enough to make naive compression expand, and verifies `compression.applied=false` rather than a larger `compression.stdout`. (verification: integration - `cargo test --test integration compression_expansion_guard` fails against the pre-guard implementation)

- [x] Ensure useful compression still applies when it is smaller. Completion condition: repeated-line log output or error-focused output still returns `compression.applied=true` with compact text when the compressed text is smaller than raw. (verification: integration - `tests/integration.rs` includes or updates a repeated/noisy output case and passes with `cargo test --test integration compression_smaller_output_still_applies`)

- [x] Run final repository verification. Completion condition: formatting, linting, and test suite pass locally. (verification: integration - run `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test --all`)

## Future Work

- Consider exposing a separate metric for skipped compression attempts if users later need diagnostics.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate prevent-compression-expansion --archive-gate`.

## Acceptance #1 Failure Follow-up
- [x] Spec requirement is per observed stream, but implementation guards only the combined stdout+stderr payload. `openspec/changes/prevent-compression-expansion/specs/agent-exec-run/spec.md:14` and `proposal.md:41` require compressed text not be included when it is larger than or equal to the raw observed stream text it summarizes. `src/compress.rs:156-158` computes `raw_bytes = raw_stdout.len() + raw_stderr.len()` and `compressed_bytes = data.stdout.len() + data.stderr.len()`, so an expanded `compression.stdout` can still be returned if `compression.stderr` shrinks enough to make the combined total smaller. Fix by applying the expansion guard independently per stream, or by suppressing all compressed text when either non-empty stream candidate is >= its corresponding raw stream, and add a regression test for asymmetric stdout/stderr sizes. Verification: `cargo test compress` passed via agent-exec job `c5d3c7740890f47b373cb2d1a8d1ce6d`; `cargo test --test integration compression_expansion_guard` passed via job `38c3e2f12407ab4224f56fc7881bbd08`.
- [x] Verification gap: existing unit tests in `src/compress.rs:366-418` cover only single-stream or combined smaller cases, and integration tests in `tests/integration.rs:7371-7429` cover NDJSON tail guard and useful smaller compression but not the asymmetric case where one stream expands while the other shrinks. Add a test that would fail under the current combined-size guard. Verification: added unit regression `expansion_guard_suppresses_when_one_stream_expands` and integration regression `compression_expansion_guard_applies_per_stream`; `cargo test compress`, `cargo test --test integration compression_expansion_guard`, and `cargo test --test integration compression_smaller_output_still_applies` passed via agent-exec jobs `c5d3c7740890f47b373cb2d1a8d1ce6d`, `38c3e2f12407ab4224f56fc7881bbd08`, and `e6e925484ace53f2a28cc84985868620`.

## Acceptance #2 Failure Follow-up
- [x] Implementation behavior for the previous per-stream expansion finding is fixed in code and covered by tests: `src/compress.rs:156-158` now computes `stdout_expands` and `stderr_expands` independently and suppresses all compressed text when either non-empty stream candidate is `>=` its raw stream; regression tests exist at `src/compress.rs:421` and `tests/integration.rs:7432`. Targeted verification passed with runnable commands `cargo test compress`, `cargo test --test integration compression_expansion_guard`, and `cargo test --test integration compression_smaller_output_still_applies` via agent-exec jobs `c3fc9465bf99fb5c698192a8fc52cc66`, `70ccb56fdc83a6249a9fcd3ccef98e25`, and `98731271c8772d2ca2f4f888ead3f804`. (verification: integration - runnable commands `cargo test compress`, `cargo test --test integration compression_expansion_guard`, and `cargo test --test integration compression_smaller_output_still_applies` cover `src/compress.rs:156-158`, `src/compress.rs:421`, and `tests/integration.rs:7432`.)

## Acceptance #3 Failure Follow-up
- [x] Confirm the previous per-stream expansion behavior fix and regression coverage remain present: `src/compress.rs:156-158` checks `stdout_expands` and `stderr_expands` independently and suppresses compressed text when either non-empty stream candidate is `>=` its raw stream; regression coverage exists at `src/compress.rs:421` and `tests/integration.rs:7432`. (verification: source-and-test-inspection - re-read `src/compress.rs` and `tests/integration.rs` and verified the per-stream guard plus unit/integration regression coverage are present.)

## Acceptance #4 Failure Follow-up
- [x] Active task checkbox status is otherwise complete: `openspec/changes/prevent-compression-expansion/tasks.md` has no unchecked `- [ ]` items. The implementation evidence still satisfies the behavior requirement: `src/compress.rs:156-158` checks `stdout_expands` and `stderr_expands` independently and suppresses compressed text when either non-empty stream candidate is `>=` its raw stream; regression coverage exists at `src/compress.rs:421` and `tests/integration.rs:7432`. (verification: source-and-test-inspection - re-read `src/compress.rs`, `tests/integration.rs`, and `openspec/changes/prevent-compression-expansion/tasks.md`.)
- [x] Remove self-referential checkbox tasks from Acceptance #2 and Acceptance #3 so active tasks cite source and test evidence instead of treating archive validation itself as implementation evidence. (verification: integration - repository evidence remains in `src/compress.rs:156-158`, unit regression coverage remains in `src/compress.rs:421`, integration regression coverage remains in `tests/integration.rs:7432`, and archive validation text is isolated in the non-checkbox `openspec/changes/prevent-compression-expansion/tasks.md:19-22` Final Validation section.)

## Acceptance #5 Failure Follow-up
- [x] Remove literal unchecked-checkbox text from completed task notes so checkbox detection sees only real active task items. The implementation evidence still satisfies the behavior requirement: `src/compress.rs:156-158` checks `stdout_expands` and `stderr_expands` independently and suppresses compressed text when either non-empty stream candidate is `>=` its raw stream; regression coverage exists at `src/compress.rs:421` and `tests/integration.rs:7432`. (verification: integration - `cargo test compress` covers the per-stream guard unit regression in `src/compress.rs:421`, and `cargo test --test integration compression_expansion_guard` covers guarded response behavior in `tests/integration.rs:7432`.)
- [x] Rewrite Acceptance #4 verification ownership into accepted terms and repository-verifiable evidence. The previous failure from agent-exec job `e6d247e5e2d54882b827c53806c1a287` cited stale verification wording; the current note cites repository evidence in `src/compress.rs:156-158`, `src/compress.rs:421`, and `tests/integration.rs:7432`. (verification: integration - runnable command `cargo test compress` verifies the unit regression in `src/compress.rs:421`; runnable command `cargo test --test integration compression_expansion_guard` verifies the integration regression in `tests/integration.rs:7432`.)

## Acceptance #6 Failure Follow-up
- [x] Active task checkbox status is complete: real unchecked task items were removed or completed in `openspec/changes/prevent-compression-expansion/tasks.md`. The implementation evidence still satisfies the behavior requirement: `src/compress.rs:156-158` checks `stdout_expands` and `stderr_expands` independently and suppresses compressed text when either non-empty stream candidate is `>=` its raw stream; regression coverage exists at `src/compress.rs:421` and `tests/integration.rs:7432`. (verification: integration - runnable command `cargo test --test integration compression_expansion_guard` passed in agent-exec job `c21ef976324d3ceb680f62a8e2453fb2`.)
- [x] Rewrite the Acceptance #5 second follow-up verification note so it cites concrete repository-verifiable evidence and runnable commands accepted by task-note validation. The fixed note cites `src/compress.rs:156-158`, `src/compress.rs:421`, `tests/integration.rs:7432`, runnable command `cargo test compress`, and runnable command `cargo test --test integration compression_expansion_guard`. (verification: integration - runnable command `cargo test --test integration compression_expansion_guard` passed in agent-exec job `c21ef976324d3ceb680f62a8e2453fb2`; source evidence is in `src/compress.rs:156-158`; integration assertion evidence is in `tests/integration.rs:7432`.)

## Acceptance #7 Failure Follow-up
- [x] Archive gate itself is now fixed: `agent-exec run -- cflx openspec validate prevent-compression-expansion --archive-gate` job `bb587191210a1b2e5e37588309f43a3c` exited 0 with validation passed, and the pre-commit hook path check ran without hook failures. Active task checkbox status is complete: no real unchecked task items were found. Behavior evidence for the compression requirement is present: `src/compress.rs:156-158` applies per-stream expansion checks; unit regression exists at `src/compress.rs:421`; integration regression exists at `tests/integration.rs:7432`; targeted jobs `5bbf08ec0a407bc4f703841559dd2d1f`, `5bd58114010d09b5e4d3fb9abaf8f314`, and `df7ad35b8170ee461291aa7427185d65` all exited 0. PASS is now unblocked by final verification below.
- [x] Required final repository verification passes: the previous `cargo test --all` failure was not reproduced. Targeted regression `agent-exec run -- cargo test --test integration run_progress_every_updates_state -- --nocapture` job `f6ad79b5a4aae3ca4f6763b5c5a98354` exited 0, and full verification `agent-exec run -- cargo test --all` job `b3ab0e5ee385412ebcbcab0691993164` exited 0 with `240 passed; 0 failed` for `tests/integration.rs` and all serve integration tests passing.
- [x] Required final repository formatting verification passes: applied `cargo fmt --all` in job `3d170452336e8837d7b0ffbcb4855bcb`, which formatted the long compressed-byte assertions in `tests/integration.rs:7448-7455`; `agent-exec run -- cargo fmt --all -- --check` job `e3f502bd75259b31518e1fd6f23f8d26` exited 0.
