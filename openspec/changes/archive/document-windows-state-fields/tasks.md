## Specification Tasks

- [x] Promote `specs/agent-exec-windows/spec.md` delta — state.json に `windows_job_name` を MUST 化 (Expected canonical result: canonical spec が `windows_job_name: string` 形式 `AgentExec-{job_id}` を明記)

## Future Work

- なし
