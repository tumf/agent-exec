## Specification Tasks

- [x] Promote `specs/agent-exec/spec.md` delta — start の既定 inline 観測契約を MUST として再掲 (expected: canonical spec が `start` の既定 wait=true, until=10, max-bytes=65536 を明記し `run` と同じ inline output 契約を共有することを示す)

## Future Work

- なし
