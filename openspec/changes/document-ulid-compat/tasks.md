## Specification Tasks

- [ ] Promote `specs/agent-exec-jobstore/spec.md` delta — ULID/hex 併存時の prefix 解決ルール
  - Expected canonical result: canonical spec が「新規は hex、既存読み取りは形式を問わない」「prefix 衝突時は暗黙順位を置かず ambiguous_job_id に全候補」を明記

## Future Work

- なし
