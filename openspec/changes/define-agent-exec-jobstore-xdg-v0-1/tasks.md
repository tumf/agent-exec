## 1. パス解決とジョブ構造

- [ ] 1.1 ジョブ保存先の解決ロジックを実装する（検証: 解決順序のユニットテストが追加される）
- [ ] 1.2 ジョブディレクトリとログ/JSONファイルの生成を実装する（検証: 実行後に `meta.json`/`state.json`/ログが生成される）

## 2. meta/state の内容と書き込み

- [ ] 2.1 `meta.json` の必須フィールドと env_keys のみ保存を実装する（検証: `meta.json` に env 値が含まれない）
- [ ] 2.2 `state.json` の必須フィールドと更新を実装する（検証: `state.json` に `updated_at` が含まれる）
- [ ] 2.3 `meta.json` と `state.json` の原子的な書き込みを実装する（検証: 一時ファイル経由の書き込みが行われる）
